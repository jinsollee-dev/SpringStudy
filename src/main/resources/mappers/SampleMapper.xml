<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ljs.springexam01.mapper.SampleMapper">

    <select id="getList" resultType="org.ljs.springexam01.dto.SampleDTO">
        select * from tbl_sample
    </select>
<!--    <insert id="insert">-->
<!--        inset int tbl_sample(name, age, phone) values(#{name}, #{age}, #{phone})-->
<!--    </insert>-->
</mapper>